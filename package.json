{
  "name": "src",
  "version": "0.1.20",
  "description": "",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "add-version": "npm version patch",
    "build-js": "replace '@VERSION@' $npm_package_version build.js && node ./build.js",
    "build": "npm run add-version && mkdir -p build && npm run build-js && npm run post-build",
    "post-build": "replace $npm_package_version '@VERSION@' build.js",
    "build:development": "",
    "build:production": ""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/devDan118/novote.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/devDan118/novote/issues"
  },
  "homepage": "https://github.com/devDan118/novote#readme",
  "dependencies": {
    "adm-zip": "^0.4.11"
  }
}
